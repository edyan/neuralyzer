guesser_version: 1.0.0b
entities:
    guestbook:
        cols:
            username: { method: sentence, params: [8] }
            content: { method: sentence, params: [20] }
            created: { method: datetime, params: ['now'] }
